# ===========================================
# BLOG APPLICATION ENVIRONMENT CONFIGURATION
# ===========================================

# Database Configuration
# -----------------------
DB_HOST=localhost
DB_USER=admin
DB_PASSWORD=password123
DB_NAME=blog_db
DB_PORT=3306

# AWS Configuration
# -----------------
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=your-access-key-id
AWS_SECRET_ACCESS_KEY=your-secret-access-key

# LocalStack Configuration (for development)
# ------------------------------------------
# Uncomment the line below when using LocalStack
# AWS_ENDPOINT_URL=http://localhost:4566
# Note: LocalStack Community Edition has limited service support
# cognito-idp and cloudfront are not fully supported

# Amazon Cognito Configuration
# ----------------------------
# For production use real values, for LocalStack use mock values
COGNITO_USER_POOL_ID=us-east-1_YourPoolId
COGNITO_CLIENT_ID=your-client-id

# Amazon S3 Configuration
# -----------------------
S3_BUCKET_NAME=blog-images-bucket

# Amazon CloudFront Configuration
# -------------------------------
# For LocalStack development, this points to nginx mock at localhost:8080
CLOUDFRONT_DOMAIN=d123456789.cloudfront.net

# Application Configuration
# -------------------------
PORT=3000
NODE_ENV=development

# LocalStack Specific (for Docker development)
# --------------------------------------------
LOCALSTACK_HOSTNAME=localhost

# Security Configuration
# ----------------------
SESSION_SECRET=your-session-secret-key-here

# Logging Configuration
# --------------------
LOG_LEVEL=info

# ===========================================
# DOCKER ENVIRONMENT OVERRIDES
# ===========================================
# When running with Docker Compose, these values will be overridden:
# DB_HOST=mysql
# AWS_ENDPOINT_URL=http://localstack:4566
# LOCALSTACK_HOSTNAME=localstack
